class CatalogueModel{
  CatalogueModel({
      this.text="",
      this.selected=false
  });

  final String text;
  final bool selected;

  // factory User.fromJson(Map<String, dynamic> json) => User(
  //   name: json["name"],
  // );


}